spring.application.name=bentoflash

# H2 Database Configuration
spring.datasource.url=jdbc:h2:mem:bentoflashdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Automatically create tables and format SQL logs
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Enable H2 Console web portal so you can see tables at http://localhost:8080/h2-console
spring.h2.console.enabled=true